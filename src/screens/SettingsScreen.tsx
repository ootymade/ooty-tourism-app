import { PlaceholderScreen } from '../components';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Language (English / Tamil / Hindi), offline data management, and notification preferences."
    />
  );
}
