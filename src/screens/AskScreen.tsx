import { PlaceholderScreen } from '../components';

export function AskScreen() {
  return (
    <PlaceholderScreen
      title="Ask"
      description="The AI Concierge: a full-screen chat that answers from OotyMade's verified data — never guesses on prices, timings, permits or safety."
    />
  );
}
