import { PlaceholderScreen } from '../components';

export function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Home"
      description="Today's snapshot: weather, what's open right now, your saved trip plan's progress, and a quick way into the AI concierge."
    />
  );
}
