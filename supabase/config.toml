# Supabase CLI project config. Not yet linked to a real project —
# run `supabase init` normally generates this file; this one is
# hand-written to match the minimal setup this app needs so
# `supabase start` / `supabase db reset` / `supabase functions serve`
# work once a project exists.

project_id = "ootymade-nilgiris-trip-companion"

[api]
enabled = true
port = 54321
schemas = ["public"]

[db]
port = 54322
major_version = 16

[studio]
enabled = true
port = 54323

[functions.ai-concierge]
verify_jwt = false
